# DoE — old InOx campaign design folder (empty)

**What it was:** the output folder of the original **InOx** thin-film-transistor campaign's DoE
(`doe/doe_design.ipynb` — L32 Taguchi and Sobol designs over 6 factors, with the
*Substrate Temp < Annealing Temp* ordering constraint).

The generated design tables were moved/consumed elsewhere (see `../previous_temp/` for the
`L32_design_inOX_BEOL*.xlsx` drafts); the folder is kept only so the old notebook's save path
still exists.

**Vs the others:** pre-PdO era — different material system (InOx), different factor set.
